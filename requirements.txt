# Ground Region Tracker — Python dependencies
# Recommended on Jetson:
#   python3 -m venv .venv --system-site-packages
#   source .venv/bin/activate
#   pip install -r requirements.txt

numpy>=1.23,<2.0
opencv-python>=4.8; platform_machine != "aarch64"
PyYAML>=6.0
pytest>=7.0
psutil>=5.9

# GUI — install ONE of:
#   pip install PySide6
#   sudo apt install python3-pyqt5
# The app auto-selects via gui/qt_compat.py
PySide6>=6.5; platform_machine != "aarch64"

# Optional AI stack (YOLO / TensorRT via Ultralytics)
# Install JetPack-matched torch first on Jetson, then:
#   pip install ultralytics onnx
#
# Notes:
# - On Jetson aarch64 prefer apt/system OpenCV (GStreamer-enabled).
# - TensorRT Python bindings come from JetPack.
# - Do not pin torch here; use the JetPack-matched wheel.
