# Jetson Orin Nano Runtime
# Prefer system OpenCV from JetPack. Create venv with --system-site-packages.
numpy>=1.23,<2.0
PyYAML>=6.0
pytest>=7.0
psutil>=5.9
# GUI (pick one):
PySide6>=6.5
# Optional inference stack (after JetPack-matched torch if needed):
# ultralytics
# onnx
